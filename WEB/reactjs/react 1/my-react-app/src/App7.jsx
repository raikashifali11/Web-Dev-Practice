// click event = An interaction when a user clicks on a specific element.
// We can respond to clicks by passing
// a callback to the onClick event handlers

import Button from './Button2.jsx'
function App() {

	return(
		<>
      <Button/>
		</>
	);
}

export default App
